name: Backup Repository to SharePoint

on:
  workflow_call:
    inputs:
      retention_count:
        description: Number of backups to retain
        required: false
        type: number
        default: 5
    secrets:
      SHAREPOINT_CLIENT_ID:
        required: true
      SHAREPOINT_CLIENT_SECRET:
        required: true
      AZURE_TENANT_ID:
        required: true

  workflow_dispatch:
    inputs:
      retention_count:
        description: Number of backups to retain
        required: false
        type: number
        default: 5

  schedule:
    - cron: "35 1 * * *"

permissions:
  contents: read

jobs:
  backup:
    name: Backup to SharePoint
    runs-on: ubuntu-latest
    env:
      SP_CLIENT_ID: ${{ secrets.SHAREPOINT_CLIENT_ID }}
      SP_CLIENT_SECRET: ${{ secrets.SHAREPOINT_CLIENT_SECRET }}
      AZ_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
    steps:
      - name: Backup repository to SharePoint
        uses: ministryofjustice/hmpps-github-shared-actions/.github/actions/sharepoint_repository_backup@97eeedc606b4ce163133d203259a71698c9ad34b # v1.0.16
        with:
          retention_count: ${{ inputs.retention_count || 5 }}
