name: Backup Repository to SharePoint

on:
  workflow_call:
    inputs:
      retention_count:
        description: Number of backups to retain
        required: false
        type: number
        default: 5
    secrets:
      SHAREPOINT_CLIENT_ID:
        required: true
      SHAREPOINT_CLIENT_SECRET:
        required: true
      AZURE_TENANT_ID:
        required: true

  workflow_dispatch:
    inputs:
      retention_count:
        description: Number of backups to retain
        required: false
        type: number
        default: 5

  schedule:
    - cron: "35 1 * * *"

permissions:
  contents: read

jobs:
  backup:
    name: Backup to SharePoint
    runs-on: ubuntu-latest
    env:
      SP_CLIENT_ID: ${{ secrets.SHAREPOINT_CLIENT_ID }}
      SP_CLIENT_SECRET: ${{ secrets.SHAREPOINT_CLIENT_SECRET }}
      AZ_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
    steps:
      - name: Backup repository to SharePoint
        uses: ministryofjustice/hmpps-github-shared-actions/.github/actions/sharepoint_repository_backup@22d87963ea96458c47d7c1dff8670448a0be99ec # v1.1.1
        with:
          retention_count: ${{ inputs.retention_count || 5 }}
